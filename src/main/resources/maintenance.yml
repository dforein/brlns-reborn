# true/false
# if 'true', the whitelist is enabled (put only staff players) and the maintenance commands are made available.
server-under-maintenance: true

# true/false
# if 'true', all levels are loaded and the minigame maintenance commands are made available.
# this works only if 'server-under-maintenance' is 'true'
load-all-levels: true